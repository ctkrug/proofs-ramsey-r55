# R(5,5) epoch 3 attempt

Outcome: internal research-software progress; strategy remains blocked. No new
Ramsey bound, witness, classification, target-order exclusion, or canonical
novelty claim is made.

The new Hugging Face resolve-path acquisition failed DNS resolution with curl
code 6 and left no raw corpus or HTTP headers. The final target preflight then
returned code 2 with `downstream_checks_run=false`, so zero order-42 records
were evaluated.

The reusable improvement is adjacency-level parser verification. Checker A,
Checker B, and NetworkX now emit/derive complete upper-triangle fingerprints.
The final exhaustive order-7 regression passed on all 1,044 unlabeled classes
and their complements, with exact evaluator agreement and byte-exact nauty
complementation. The provisional K43 semantic mutations and malformed-input
controls also passed, without changing its unauthenticated status.

Exact continuation: obtain unchanged McKay bytes through a genuinely new raw
file route, preserve provenance, and run the command in `CHECKPOINT.md`. Do not
repeat either failed curl path in the unchanged command environment.
