## Literature-strategy memo

**Best live route:** close the source-control gate. No search route is currently admissible. Once both raw-source gates pass, prioritize the authenticated six-vertex K43 repair slice (S4), with unknown-42 discovery (S3) as its novelty test—not direct extension of known controls.

**Rationale:** The maintained survey is revision 18 (24 April 2026) and still records \(43\le R(5,5)\le46\). It calls the 656 order-42 graphs strong evidence, not a classification. [Dynamic Survey](https://www.cs.rit.edu/~spr/ElJC/sur.pdf) McKay–Radziszowski explicitly checked that none of those 656 extends to 43; hence every vertex deletion of any genuine K43 witness must be an *unknown* order-42 class. This is a direct inference, not census completeness. [Primary source](https://users.cecs.anu.edu.au/~bdm/papers/r55.pdf)

The local control packet is useful but incomplete: graph6 plumbing passed only at order 7, and the two-conflict K43 result applies to a third-party transcription with open publisher provenance. The raw McKay corpus is absent, so the fail-closed corpus gate correctly ran no downstream checks.

**Cheapest discriminator:** obtain unchanged `r55_42some.g6`, retain retrieval metadata, and require all of:

- 47,888 bytes; SHA-256 `067902…0eccb`; 328 LF-terminated records;
- dual exact validation of the 328 records and complements;
- the published edge histogram, degree range 19–22, complement involution, and 656 distinct nauty labels.

Any mismatch ends the route. Only then obtain and hash Springer Supplementary Data 4, decode its color convention, and require the reported two bad K5 identities, six-vertex union, and four valid deletion controls.

**Post-gate prediction and test:** source concept: the published two-conflict K43 has highly overlapping violations. Prediction: exact bounded repair around its six-vertex support will be more informative than generic local search. Test: freeze a declared exterior, certify increasing destroy-set neighborhoods, and compare against matched-budget tabu/annealing. A zero passes only after dual checking; UNSAT proves only that declared neighborhood.

**Controls:** retain the current two independent checkers, semantic mutations, and graph6 regression; reconstruct the public 13- and 9-conflict K43 controls before interpreting a recovered two-conflict parser. Canonicalize each valid K43 deletion against the authenticated 656, but describe non-membership only as novelty relative to the supplied controls.

**Failure modes:** treating the mirror hash as authority authentication; treating the 656 as exhaustive; using the stale data-page “43–47” language as current status; claiming bounded-neighborhood UNSAT or a heuristic minimum of two is global; or restarting historical annealing/tabu/common-subgraph searches without a novelty discriminator. The historical paper records those searches as saturated, including over 65 million generated order-42 outputs. [Primary source](https://users.cecs.anu.edu.au/~bdm/papers/r55.pdf)

**Reusable artifact:** an immutable control packet containing raw bytes, provenance, per-record hashes, checker outputs listing forbidden 5-sets, complement and canonical manifests, supplementary-seed decoding, and CNF/proof-log schemas for bounded repair.

**Stop condition:** stop immediately at any source, parser, checker, histogram, complement, or canonical-label mismatch. Do not start S3/S4 until both raw-source gates pass.

**What Sol should reject or independently verify:** reject any candidate without two exact evaluations and authenticated provenance; reject any “new 42” claim lacking canonical comparison to the authenticated control set; and independently verify the logical deletion implication, source byte hashes, K43 color convention, and the scope of every SAT/UNSAT claim. No files were modified.
