## Literature-strategy memo

**Best live route:** close the raw-source control gate; no target-order search is currently admissible. The current survey is DS1.18 (24 April 2026) and still gives \(43\le R(5,5)\le46\). [Dynamic Survey](https://www.cs.rit.edu/~spr/ElJC/sur.pdf) The 2025 revision of Angeltveit–McKay likewise proves only \(R(5,5)\le46\). [Paper](https://arxiv.org/abs/2409.15709)

**Exact rationale:** the workspace has no authenticated `r55_42some.g6`; the fail-closed gate therefore performed zero order-42 evaluations. The existing parser/checker result is only an order-7 plumbing validation. McKay’s maintained page identifies 328 supplied 42-vertex representatives plus complements, while explicitly allowing further 42-vertex and 43–47-vertex graphs; it is not a classification. [Data page](https://users.cecs.anu.edu.au/~bdm/data/ramsey.html)

Historically, none of the known 656 extends to 43. Thus, if a 43-vertex witness exists, each of its 43 vertex-deletions is a valid but unknown 42-vertex graph. This is a direct conditional inference, not a completeness claim. [McKay–Radziszowski](https://users.cecs.anu.edu.au/~bdm/papers/r55.pdf) Hence direct extension of the known corpus and generic rediscovery search remain ruled out.

**Cheapest discriminator:** obtain unchanged McKay corpus bytes via a genuinely new access path or supplied raw artifact with provenance, then run the recorded corpus gate exactly once.

Required admission conditions:

- 47,888 bytes; 328 nonblank LF-terminated records;
- SHA-256 `067902e853d87b49bcef0d1d4c0e3bbadd238ee18bc65341b079a3ca4780eccb`;
- two independent exact forbidden-set evaluators, NetworkX signatures, complement involution, published edge/degree checks, and 656 distinct canonical labels.

This hash is an acquisition discriminator, not source authentication by itself: preserve retrieval time, final URL, headers/content type, and original bytes. Any discrepancy is a result to preserve, not normalize away.

**Controls:** retain the current three-parser/order-7 regression, semantic mutations, and independent checker outputs. If the corpus gate passes, acquire and hash Springer Supplementary Data 4 before using the two-conflict K43 transcription. Check its color convention and exact two \(K_5\) identities under both evaluators. Reconstruct the public 13- and 9-conflict controls from the Exoo study as parser/evaluator controls; that study supports “very few” K43 monochromatic \(K_5\) variants, not a witness. [Ge et al.](https://arxiv.org/abs/2212.12630)

**Post-gate strongest research route:** authenticated six-vertex conflict-core repair (S4), with canonicalization of the four valid K43 deletions against the supplied 656 as the novelty control. Prediction: because the two recorded violations overlap on six vertices, bounded coordinated repair is more discriminating than one-edge local search. Test increasing declared destroy sets, freeze the exterior, certify each bounded optimum/UNSAT scope, and compare against matched-budget tabu/annealing. A zero requires dual exact validation; UNSAT proves only that declared neighborhood.

**Failure modes:**

- treating the mirror digest as authoritative provenance;
- treating the 656 as exhaustive;
- treating the stale corpus-page “43–47” wording as current bounds;
- calling bounded-neighborhood UNSAT or a heuristic burden of two a global result;
- resuming historical annealing/tabu/common-subgraph searches without a novelty discriminator;
- interpreting the third-party K43 transcription as Springer supplementary data.

**Reusable artifact:** immutable source/provenance packet: raw corpus and supplement, retrieval metadata, per-record hashes, exact violation lists from both checkers, NetworkX/canonical/complement manifests, decoded K43 convention, plus deterministic CNF and proof-log schema for bounded repair.

**Stop condition:** stop at any byte, record-count, parser, forbidden-set, signature, histogram, degree, complement, or canonical-label mismatch. Do not start S3 unknown-42 discovery or S4 repair until both raw-source gates pass.

**What Sol should independently verify/reject:** independently verify the deletion implication, source-byte provenance and hashes, corpus-gate implementation hashes, K43 color convention, and every claimed SAT/UNSAT neighborhood boundary. Reject any “new 42” without canonical comparison to the authenticated supplied controls, and reject any candidate without two exact evaluations. No files were modified.
