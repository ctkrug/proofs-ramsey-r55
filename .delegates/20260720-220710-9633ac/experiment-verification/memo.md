Memo saved: [experiment-verification memo](/root/proof-factory/research/ramsey-r55/workspace/docs/delegate-memos/20260720-221349-experiment-verification.md).

Key finding: the newly present corpus bytes match the frozen 47,888-byte SHA-256, but its sidecar infers `content_type` rather than recording it from a response header. That fails the checkpoint’s provenance standard. The next decisive action is a second authoritative raw retrieval with immutable header capture, then one non-overlapping full corpus-gate run.

No Ramsey claim or target-instance result was made.
