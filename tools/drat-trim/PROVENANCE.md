# drat-trim provenance

Retrieved from <https://github.com/marijnheule/drat-trim.git> on 2026-07-21.

Pinned upstream revision: `2e3b2dc0ecf938addbd779d42877b6ed69d9a985`.

The epoch-8 two-orbit gate compiles `drat-trim.c` with `gcc -std=c99 -O2`
inside a temporary directory and records both source and binary SHA-256 hashes.
The upstream `LICENSE` is retained. No local source modification was made.
