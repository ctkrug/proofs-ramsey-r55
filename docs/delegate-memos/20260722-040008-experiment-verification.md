## Verification memo — R(5,5)

Provenance: Terra delegate `experiment-verification`, GPT-5.6-Terra, high effort,
run `20260722-040008-2a87e5`. The source memo SHA-256 was
`f3c1429881f0f242d1b3c84e8e02b2afc3897766ee0068e29a597466d08fe1d4`.
This promoted copy records the advisory lead used by the Sol principal; none
of its claims are treated as evidence without local reproduction.

**Best live route (proposal):** run the predeclared record-21 frozen-core
residual-SAT discriminator, not another census. The complete supplied-corpus
census now validates 656 canonical hosts, exact mixed 30/60-second gates, 6
positive hosts, 12 vector occurrences, and 8 distinct 426-bit vectors. This
remains only a supplied-656-class boundary experiment, not a Ramsey result.

**Cheapest decisive experiment:** one seed-1 CaDiCaL run, capped at 3,600
seconds, 2 GiB, and a 6 GiB proof file, followed by proof/model checks. Bind
the producer, graph checkers, corpus report, census manifest and validation,
solver, and a fresh build of pinned drat-trim. A timeout, checker failure, or
proof-size hit is `UNKNOWN`.

**Controls:** before solving, flip one retained vector bit, alter a manifest
hash, and replace one block-clause literal; each corruption must be rejected.
For SAT, evaluate the physical DIMACS model, run both graph checkers,
canonicalize with nauty, and run the cold auditor. For UNSAT, require both the
production and cold drat-trim checks. The cold audit must freshly replay all
656 hosts because the standalone census validator checks stored
dual-enumerator agreement and integrity, not a fresh semantic census.

**Search efficiency:** row-lex canonicalization quotients residual-label
symmetry; gzip and a deduplicated ledger compress artifacts; all eight unique
blocks are batched into one formula; the order-30 core is frozen; malformed
vectors and unions above 10,000 fail closed. Deduplication reduces 12 retained
vector occurrences to 8 clauses, while census reuse avoids an estimated 4–6
core-hours of repeat enumeration.

**Stop condition:** stop on any hash, preflight, control, or audit failure;
otherwise stop after the single bounded solve and independent audit, without
parameter retuning. Never claim that this exhausts order-42 graphs, that corpus
nonmembership establishes novelty, or that an unchecked solver verdict is a
mathematical result.
